<%
    const advisoryCategories = ['Crime Advisory', 'Traffic Advisory', 'Cybercrime Advisory', 'Community Awareness'];
    const advisoryCount = bulletins.filter(b => advisoryCategories.includes(b.category)).length;
    const newsCount = bulletins.filter(b => b.category === 'General Announcement').length;
%>
<div class="space-y-8 animate-fade-in px-4 lg:px-4 max-w-7xl mx-auto w-full transition-all duration-300">
    <!-- Header with Stats -->
    <div class="w-full transition-all duration-300">
        <div class="bg-white dark:bg-slate-900 border border-gray-150 dark:border-slate-800/60 shadow-sm hover:shadow-2xl p-6 md:p-8 rounded-[2.5rem] flex flex-col lg:flex-row items-center justify-between lg:space-x-8 space-y-6 lg:space-y-0 group w-full transition-all duration-300">
            <!-- Icon & Stats -->
            <div class="flex flex-col sm:flex-row items-center space-y-4 sm:space-y-0 sm:space-x-8 w-full lg:w-auto justify-center lg:justify-start">
                <div class="flex items-center space-x-4">
                    <div class="w-12 h-12 bg-pnp-blue rounded-[1.25rem] flex items-center justify-center text-pnp-gold shadow-2xl rotate-3 border-2 border-white/10 group-hover:rotate-0 transition-transform duration-500 shrink-0">
                        <i class="fas fa-shield-halved text-lg"></i>
                    </div>
                    <div>
                        <h4 class="text-[9px] font-black text-pnp-blue dark:text-blue-300 uppercase tracking-[0.15em] leading-none opacity-80">Public Advisories</h4>
                        <div class="text-2xl font-black text-slate-900 dark:text-white mt-1 tabular-nums leading-none"><%= String(advisoryCount).padStart(2, '0') %></div>
                        <p class="text-[8px] text-slate-400 dark:text-blue-300/50 mt-1 uppercase font-bold tracking-widest">Active Safety Alerts</p>
                    </div>
                </div>

                <div class="hidden sm:block h-8 w-[1px] bg-gray-200 dark:bg-white/10"></div>

                <div class="flex items-center space-x-4">
                    <div class="w-12 h-12 bg-pnp-blue rounded-[1.25rem] flex items-center justify-center text-pnp-gold shadow-2xl rotate-3 border-2 border-white/10 group-hover:rotate-0 transition-transform duration-500 shrink-0">
                        <i class="fas fa-newspaper text-lg"></i>
                    </div>
                    <div>
                        <h4 class="text-[9px] font-black text-pnp-blue dark:text-blue-300 uppercase tracking-[0.15em] leading-none opacity-80">News & Releases</h4>
                        <div class="text-2xl font-black text-slate-900 dark:text-white mt-1 tabular-nums leading-none"><%= String(newsCount).padStart(2, '0') %></div>
                        <p class="text-[8px] text-slate-400 dark:text-blue-300/50 mt-1 uppercase font-bold tracking-widest">Published Articles</p>
                    </div>
                </div>
            </div>
            
            <!-- Publish Button -->
            <div class="w-full lg:w-auto flex justify-center">
                <button type="button" onclick="openPublishNoticeModal()" class="w-full lg:w-auto px-6 py-3.5 bg-pnp-blue hover:bg-pnp-gold text-white hover:text-pnp-blue rounded-xl text-[10px] font-black uppercase tracking-[0.2em] shadow-lg transition-all transform hover:scale-[1.05] active:scale-95 flex items-center justify-center space-x-2 whitespace-nowrap cursor-pointer">
                    <i class="fas fa-plus"></i>
                    <span>Publish Notice</span>
                </button>
            </div>
        </div>
    </div>

    <!-- Bulletin Grid -->
    <div class="bg-white dark:bg-slate-900 rounded-[2.5rem] shadow-2xl border border-gray-100 dark:border-slate-800 overflow-hidden min-h-[500px]">
        <div class="p-8 border-b border-gray-50 dark:border-slate-800 bg-gray-50/10 dark:bg-slate-900/50 flex flex-col lg:flex-row justify-between items-center gap-6">
            <div>
                <h3 class="font-black text-pnp-blue dark:text-pnp-gold uppercase tracking-[0.3em] text-sm">Directives</h3>
            </div>
            <div class="flex flex-col sm:flex-row items-center gap-4 w-full lg:w-auto">
                <!-- Dropdown -->
                <div class="relative w-full sm:w-56">
                    <select id="bulletinCategoryFilter" onchange="filterBulletins()" class="w-full pl-5 pr-10 py-4 bg-gray-50 dark:bg-slate-950 border border-gray-100 dark:border-slate-800 rounded-2xl outline-none text-[10px] font-black text-slate-600 dark:text-white uppercase tracking-widest focus:ring-2 focus:ring-pnp-blue/20 transition-all cursor-pointer appearance-none">
                        <option value="All" class="bg-white dark:bg-slate-950 text-slate-800 dark:text-white font-black" <%= (typeof category === 'undefined' || !category || category === 'All') ? 'selected' : '' %>>All Categories</option>
                        <option value="Crime Advisory" class="bg-white dark:bg-slate-950 text-slate-800 dark:text-white font-black" <%= (typeof category !== 'undefined' && category === 'Crime Advisory') ? 'selected' : '' %>>Crime Advisory</option>
                        <option value="Traffic Advisory" class="bg-white dark:bg-slate-950 text-slate-800 dark:text-white font-black" <%= (typeof category !== 'undefined' && category === 'Traffic Advisory') ? 'selected' : '' %>>Traffic Advisory</option>
                        <option value="Cybercrime Advisory" class="bg-white dark:bg-slate-950 text-slate-800 dark:text-white font-black" <%= (typeof category !== 'undefined' && category === 'Cybercrime Advisory') ? 'selected' : '' %>>Cybercrime Advisory</option>
                        <option value="Community Awareness" class="bg-white dark:bg-slate-950 text-slate-800 dark:text-white font-black" <%= (typeof category !== 'undefined' && category === 'Community Awareness') ? 'selected' : '' %>>Community Awareness</option>
                        <option value="General Announcement" class="bg-white dark:bg-slate-950 text-slate-800 dark:text-white font-black" <%= (typeof category !== 'undefined' && category === 'General Announcement') ? 'selected' : '' %>>General Announcement</option>
                    </select>
                    <i class="fas fa-chevron-down absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 pointer-events-none text-xs"></i>
                </div>
                <!-- Search -->
                <div class="relative w-full lg:w-72">
                    <i class="fas fa-search absolute left-4 top-1/2 -translate-y-1/2 text-gray-300 text-xs"></i>
                    <input type="text" id="bulletinSearchInput" onkeyup="filterBulletins()" placeholder="Search mission labels..." class="w-full pl-10 pr-4 py-4 bg-gray-50 dark:bg-slate-950 border border-gray-100 dark:border-slate-800 rounded-2xl outline-none text-[10px] font-black text-slate-600 dark:text-white uppercase tracking-widest focus:ring-2 focus:ring-pnp-blue/20 transition-all placeholder:text-gray-300">
                </div>
            </div>
        </div>

        <div class="p-6">
            <!-- Empty state placeholder (visible when no items match) -->
            <div id="emptyStatePlaceholder" class="py-32 flex flex-col items-center justify-center text-center hidden">
                <i class="fas fa-bullhorn text-6xl mb-6 text-gray-200 dark:text-slate-800"></i>
                <h3 class="text-xl font-black text-gray-400 dark:text-gray-500 uppercase tracking-widest">No active directives found</h3>
                <p class="text-gray-400 dark:text-gray-500 text-sm mt-2">Publish a new notice or adjust filter criteria.</p>
            </div>

            <div id="bulletinList" class="flex flex-col gap-4 transition-all duration-300">
                <% if (bulletins.length === 0) { %>
                    <div class="py-32 flex flex-col items-center justify-center text-center">
                        <i class="fas fa-bullhorn text-6xl mb-6 text-gray-200 dark:text-slate-800"></i>
                        <h3 class="text-xl font-black text-gray-400 dark:text-gray-500 uppercase tracking-widest">No active directives found</h3>
                        <p class="text-gray-400 dark:text-gray-500 text-sm mt-2">Publish a new notice to broadcast.</p>
                    </div>
                <% } else { %>
                    <% bulletins.sort((a,b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime()).forEach(bulletin => { %>
                        <div data-category="<%= bulletin.category %>" class="group bg-white dark:bg-slate-900/50 rounded-3xl p-6 shadow-sm border border-gray-100 dark:border-slate-800 hover:shadow-xl transition-all duration-300 flex flex-col lg:flex-row items-start lg:items-center gap-6 relative overflow-hidden">
                            <!-- Thumbnail -->
                            <% if (bulletin.video_paths && bulletin.video_paths.length > 0) { %>
                                <div class="w-full lg:w-32 h-40 lg:h-24 rounded-2xl overflow-hidden shrink-0 bg-black relative">
                                    <video src="<%= bulletin.video_paths[0] %>" muted class="w-full h-full object-cover group-hover:scale-110 transition duration-700 pointer-events-none"></video>
                                    <div class="absolute inset-0 bg-black/25 flex items-center justify-center">
                                        <i class="fas fa-play text-white text-xl"></i>
                                    </div>
                                </div>
                            <% } else if (bulletin.photo_paths && bulletin.photo_paths.length > 0) { %>
                                <div class="w-full lg:w-32 h-40 lg:h-24 rounded-2xl overflow-hidden shrink-0 bg-slate-100 dark:bg-slate-800">
                                    <img src="<%= bulletin.photo_paths[0] %>" alt="<%= bulletin.title %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-700">
                                </div>
                            <% } else if (bulletin.photo_path && bulletin.photo_path !== '[]' && !bulletin.photo_path.startsWith('[')) { %>
                                <div class="w-full lg:w-32 h-40 lg:h-24 rounded-2xl overflow-hidden shrink-0 bg-slate-100 dark:bg-slate-800">
                                    <img src="<%= bulletin.photo_path %>" alt="<%= bulletin.title %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-700">
                                </div>
                            <% } else if (bulletin.facebook_url) { %>
                                <div class="w-full lg:w-48 h-40 lg:h-28 rounded-2xl overflow-hidden shrink-0 bg-slate-950 border border-[#1877F2]/30 flex items-center justify-center relative group-hover:scale-105 transition duration-500">
                                    <iframe src="https://www.facebook.com/plugins/<%= /video|watch|reel/i.test(bulletin.facebook_url) ? 'video.php' : 'post.php' %>?href=<%= encodeURIComponent(bulletin.facebook_url) %>&show_text=true&width=500" 
                                        width="100%" height="100%" style="border:none;overflow:hidden;" scrolling="no" frameborder="0" allowfullscreen="true" 
                                        allow="autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share" class="w-full h-full"></iframe>
                                </div>
                            <% } else { %>
                                <div class="w-16 h-16 rounded-2xl bg-gray-50 dark:bg-slate-800 flex items-center justify-center shrink-0 border border-gray-100 dark:border-slate-700">
                                    <i class="fas fa-bullhorn text-2xl text-gray-300 dark:text-slate-600 group-hover:text-pnp-blue transition-colors"></i>
                                </div>
                            <% } %>

                            <!-- Content -->
                            <div class="flex-grow flex flex-col w-full min-w-0">
                                <div class="flex flex-wrap items-center gap-3 mb-2">
                                    <!-- Badge -->
                                    <% 
                                        const badgeColors = {
                                            'Wanted Person': 'bg-red-500/10 text-red-500 border-red-500/10',
                                            'Missing Person': 'bg-orange-500/10 text-orange-500 border-orange-500/10',
                                            'Crime Advisory': 'bg-pnp-gold/10 text-pnp-gold border-pnp-gold/10',
                                            'Traffic Advisory': 'bg-purple-500/10 text-purple-400 border-purple-500/10',
                                            'Cybercrime Advisory': 'bg-pink-500/10 text-pink-400 border-pink-500/10',
                                            'Community Awareness': 'bg-teal-500/10 text-teal-400 border-teal-500/10',
                                            'Recovered Property': 'bg-emerald-500/10 text-emerald-500 border-emerald-500/10',
                                            'General Announcement': 'bg-pnp-blue/10 text-pnp-blue-300 border-pnp-blue/10'
                                        };
                                        const categoryColor = badgeColors[bulletin.category] || 'bg-gray-100 text-gray-500';
                                    %>
                                    <span class="px-3 py-1 rounded-full text-[9px] font-black uppercase tracking-widest border <%= categoryColor %>">
                                        <%= bulletin.category %>
                                    </span>
                                    <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                                        <i class="fas fa-clock mr-1 opacity-50"></i> <%= new Date(bulletin.created_at).toLocaleDateString(undefined, {month: 'short', day: 'numeric', year: 'numeric'}) %> &middot; <%= new Date(bulletin.created_at).toLocaleTimeString(undefined, {hour:'2-digit', minute:'2-digit'}) %>
                                    </span>
                                </div>
                                <h4 class="text-sm font-black text-slate-800 dark:text-white uppercase tracking-tight leading-tight group-hover:text-pnp-blue dark:group-hover:text-pnp-gold transition duration-300 mb-1.5 truncate">
                                    <%= bulletin.title %>
                                </h4>
                                <p class="text-[11px] text-slate-500 dark:text-slate-400 font-medium leading-relaxed line-clamp-2">
                                    <%= bulletin.body %>
                                </p>
                                <% if (bulletin.facebook_url) { %>
                                    <div class="mt-2.5">
                                        <a href="<%= bulletin.facebook_url %>" target="_blank" rel="noopener noreferrer" class="inline-flex items-center space-x-2 px-3.5 py-1.5 bg-[#1877F2] hover:bg-blue-700 text-white rounded-xl text-[10px] font-black uppercase tracking-wider transition shadow-sm hover:shadow-md">
                                            <i class="fab fa-facebook text-xs"></i>
                                            <span>View Official Facebook Post</span>
                                            <i class="fas fa-external-link-alt text-[8px] ml-1 opacity-80"></i>
                                        </a>
                                    </div>
                                <% } %>
                            </div>

                            <!-- Actions -->
                            <div class="flex items-center space-x-3 w-full lg:w-auto justify-end mt-4 lg:mt-0 shrink-0">
                                <a href="/admin/bulletins/<%= bulletin.id %>/edit" onclick="event.preventDefault(); openEditNoticeModal('<%= bulletin.id %>');" class="w-11 h-11 rounded-2xl bg-gray-50 dark:bg-slate-800 text-slate-400 hover:text-pnp-blue dark:hover:text-pnp-gold hover:bg-white dark:hover:bg-slate-700 transition-all flex items-center justify-center border border-transparent hover:border-pnp-gold/20 shadow-sm cursor-pointer" title="Edit Bulletin">
                                    <i class="fas fa-edit text-xs"></i>
                                </a>
                                <form action="/admin/bulletins/<%= bulletin.id %>/delete" method="POST" onsubmit="handleBulletinDelete(event, '<%= bulletin.id %>'); return false;">
                                    <button type="submit" class="w-11 h-11 rounded-2xl bg-gray-50 dark:bg-slate-800 text-slate-400 hover:text-red-500 hover:bg-white dark:hover:bg-slate-700 transition-all flex items-center justify-center border border-transparent hover:border-red-500/20 shadow-sm" title="Delete Bulletin">
                                        <i class="fas fa-trash-alt text-xs"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        </div>
    </div>
    <!-- Carousel Pagination Controls -->
    <div id="carouselPagination" class="p-6 border-t border-gray-50 dark:border-slate-800 flex justify-center items-center space-x-2 hidden">
        <button id="prevBtn" onclick="navigateCarousel(-1)" class="px-4 py-2 bg-gray-50 dark:bg-slate-700 hover:bg-pnp-blue hover:text-white text-gray-600 dark:text-gray-300 disabled:opacity-30 disabled:pointer-events-none rounded-lg text-[10px] font-black uppercase tracking-widest transition-all">Previous</button>
        <span id="pageIndicator" class="text-[10px] font-black uppercase tracking-widest text-gray-500 dark:text-gray-400 mx-4">Page 1 of 1</span>
        <button id="nextBtn" onclick="navigateCarousel(1)" class="px-4 py-2 bg-gray-50 dark:bg-slate-700 hover:bg-pnp-blue hover:text-white text-gray-600 dark:text-gray-300 disabled:opacity-30 disabled:pointer-events-none rounded-lg text-[10px] font-black uppercase tracking-widest transition-all">Next</button>
    </div>
</div>

<script>
    const adminBulletinsData = <%- JSON.stringify(bulletins || []).replace(/<\/script/gi, '<\\/script') %>;
    const bulletinsMap = {};
    if (Array.isArray(adminBulletinsData)) {
        adminBulletinsData.forEach(b => {
            if (b && b.id) bulletinsMap[b.id] = b;
        });
    }

    let currentPage = 1;
    const itemsPerPage = 5;

    async function handleBulletinDelete(e, id) {
        e.preventDefault();
        const confirmFn = window.strategicConfirm || window.confirmOperationalAction || (async (args) => {
            return confirm(args?.message || "Delete this entry?");
        });

        const confirmed = await confirmFn({
            title: "Delete Bulletin",
            message: "Are you sure you want to delete this bulletin? It will be moved to the Archive for easy recovery.",
            confirmText: "Move to Archive",
            cancelText: "Cancel",
            type: "danger"
        });
        
        if (confirmed) {
            e.target.closest('form').submit();
        }
    }

    window.filterBulletins = function() {
        const selectedCategory = document.getElementById('bulletinCategoryFilter').value;
        const input = document.getElementById('bulletinSearchInput').value.toLowerCase();
        
        const cards = Array.from(document.querySelectorAll('#bulletinList > .group'));
        const matchingCards = [];

        cards.forEach(card => {
            const title = card.querySelector('h4')?.textContent.toLowerCase() || '';
            const subtitle = card.querySelector('p')?.textContent.toLowerCase() || '';
            const category = card.getAttribute('data-category');
            
            const matchesSearch = title.includes(input) || subtitle.includes(input);
            const matchesCategory = selectedCategory === 'All' || category === selectedCategory;
            
            if (matchesSearch && matchesCategory) {
                matchingCards.push(card);
            } else {
                card.style.display = 'none';
            }
        });

        const totalItems = matchingCards.length;
        const totalPages = Math.ceil(totalItems / itemsPerPage);

        // Constrain current page
        if (currentPage > totalPages) {
            currentPage = Math.max(1, totalPages);
        }

        const paginationContainer = document.getElementById('carouselPagination');
        const emptyState = document.getElementById('emptyStatePlaceholder');
        const listWrapper = document.getElementById('bulletinList');

        if (totalItems === 0) {
            if (paginationContainer) paginationContainer.classList.add('hidden');
            if (emptyState) emptyState.classList.remove('hidden');
            if (listWrapper) listWrapper.classList.add('hidden');
        } else {
            if (emptyState) emptyState.classList.add('hidden');
            if (listWrapper) listWrapper.classList.remove('hidden');
            
            if (totalItems > itemsPerPage) {
                if (paginationContainer) paginationContainer.classList.remove('hidden');
            } else {
                if (paginationContainer) paginationContainer.classList.add('hidden');
            }
        }

        const startIndex = (currentPage - 1) * itemsPerPage;
        const endIndex = startIndex + itemsPerPage;

        matchingCards.forEach((card, index) => {
            if (index >= startIndex && index < endIndex) {
                card.style.display = 'flex';
                card.classList.add('animate-fade-in');
            } else {
                card.style.display = 'none';
            }
        });

        const pageIndicator = document.getElementById('pageIndicator');
        const prevBtn = document.getElementById('prevBtn');
        const nextBtn = document.getElementById('nextBtn');

        if (pageIndicator) {
            pageIndicator.textContent = `Page ${currentPage} of ${totalPages || 1}`;
        }
        if (prevBtn) {
            prevBtn.disabled = (currentPage === 1);
        }
        if (nextBtn) {
            nextBtn.disabled = (currentPage >= totalPages);
        }
    }

    window.navigateCarousel = function(direction) {
        const listContainer = document.getElementById('bulletinList');
        if (!listContainer) return;

        // Visual fade out transition
        listContainer.classList.add('opacity-0', 'scale-[0.98]');

        setTimeout(() => {
            currentPage += direction;
            window.filterBulletins();
            
            // Visual fade back in
            listContainer.classList.remove('opacity-0', 'scale-[0.98]');
        }, 150);
    }

    // Initialize carousel on document load
    document.addEventListener('DOMContentLoaded', () => {
        const initialCategory = '<%= typeof category !== "undefined" && category ? category : "All" %>';
        const filterSelect = document.getElementById('bulletinCategoryFilter');
        if (filterSelect && initialCategory) {
            filterSelect.value = initialCategory;
        }
        window.filterBulletins();

        // Check query param to auto open modal if redirected
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('openModal') === 'true') {
            openPublishNoticeModal();
        }

        const dropZone = document.getElementById('modalDropZone');
        if (dropZone) {
            dropZone.addEventListener('dragover', (e) => {
                e.preventDefault();
                dropZone.classList.add('border-pnp-blue', 'bg-pnp-blue/5');
            });
            dropZone.addEventListener('dragleave', () => {
                dropZone.classList.remove('border-pnp-blue', 'bg-pnp-blue/5');
            });
            dropZone.addEventListener('drop', (e) => {
                e.preventDefault();
                dropZone.classList.remove('border-pnp-blue', 'bg-pnp-blue/5');
                if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
                    handleModalMediaSelect(e.dataTransfer.files);
                }
            });
        }
    });

    let currentPublishType = 'advisory';
    let currentModalMode = 'create';
    let currentEditBulletinId = null;

    function openPublishNoticeModal(presetType) {
        currentModalMode = 'create';
        currentEditBulletinId = null;

        const modal = document.getElementById('publishNoticeModal');
        const box = document.getElementById('publishNoticeModalBox');
        if (!modal || !box) return;
        
        // Reset form & error state
        const form = document.getElementById('publishNoticeForm');
        if (form) {
            form.reset();
            form.action = '/admin/bulletins/create';
        }
        clearModalMedia();
        const errorEl = document.getElementById('publishFormError');
        if (errorEl) errorEl.classList.add('hidden');

        const mainTitle = document.getElementById('modalMainTitle');
        const mainSubtitle = document.getElementById('modalMainSubtitle');
        const submitBtnText = document.getElementById('publishSubmitBtnText');
        const submitBtn = document.getElementById('publishSubmitBtn');
        if (mainTitle) mainTitle.textContent = 'Publish Notice';
        if (mainSubtitle) mainSubtitle.textContent = 'Choose how you would like to publish the bulletin.';
        if (submitBtnText) submitBtnText.textContent = 'Publish Notice';
        if (submitBtn) {
            const icon = submitBtn.querySelector('i');
            if (icon) icon.className = 'fas fa-paper-plane text-xs';
        }

        if (presetType) {
            selectPublishType(presetType);
        } else {
            goToPublishStep(1);
        }

        modal.classList.remove('hidden');
        setTimeout(() => {
            modal.classList.remove('opacity-0');
            box.classList.remove('scale-95');
            box.classList.add('scale-100');
        }, 20);
    }

    function openEditNoticeModal(id) {
        const bulletin = bulletinsMap[id];
        if (!bulletin) {
            window.location.href = `/admin/bulletins/${id}/edit`;
            return;
        }

        currentModalMode = 'edit';
        currentEditBulletinId = id;

        const modal = document.getElementById('publishNoticeModal');
        const box = document.getElementById('publishNoticeModalBox');
        const form = document.getElementById('publishNoticeForm');
        const errorEl = document.getElementById('publishFormError');
        const mainTitle = document.getElementById('modalMainTitle');
        const mainSubtitle = document.getElementById('modalMainSubtitle');
        const submitBtnText = document.getElementById('publishSubmitBtnText');
        const submitBtn = document.getElementById('publishSubmitBtn');

        if (!modal || !box) return;

        if (form) {
            form.reset();
            form.action = `/admin/bulletins/${id}/edit`;
        }
        if (errorEl) errorEl.classList.add('hidden');
        if (mainTitle) mainTitle.textContent = 'Edit Bulletin Notice';
        if (mainSubtitle) mainSubtitle.textContent = 'Update bulletin details, category, or media attachments.';
        if (submitBtnText) submitBtnText.textContent = 'Update Bulletin';
        if (submitBtn) {
            const icon = submitBtn.querySelector('i');
            if (icon) icon.className = 'fas fa-save text-xs';
        }

        // Detect publication type
        let pubType = 'advisory';
        if (bulletin.facebook_url && bulletin.facebook_url.trim().length > 0) {
            pubType = 'url';
        } else if (bulletin.category === 'General Announcement') {
            pubType = 'news';
        } else {
            pubType = 'advisory';
        }

        selectPublishType(pubType);

        // Pre-fill values
        if (pubType === 'url') {
            const urlTitleInput = document.getElementById('urlTitleInput');
            const urlBodyInput = document.getElementById('urlBodyInput');
            const fbUrlInput = document.getElementById('publishFacebookUrl');

            if (urlTitleInput) urlTitleInput.value = bulletin.title || '';
            if (urlBodyInput) urlBodyInput.value = bulletin.body || '';
            if (fbUrlInput) {
                fbUrlInput.value = bulletin.facebook_url || '';
                handleFacebookUrlInput(bulletin.facebook_url || '');
            }
        } else {
            const titleInput = document.getElementById('publishTitle');
            const bodyInput = document.getElementById('publishBody');
            const categorySelect = document.getElementById('publishCategoryAdvisory');

            if (titleInput) titleInput.value = bulletin.title || '';
            if (bodyInput) bodyInput.value = bulletin.body || '';
            if (pubType === 'advisory' && categorySelect && bulletin.category) {
                categorySelect.value = bulletin.category;
            }
        }

        // Load existing media items into carousel
        clearModalMedia();

        function resolveModalMediaList(pathsVal, singleVal) {
            let list = [];
            if (Array.isArray(pathsVal)) {
                list = pathsVal;
            } else if (singleVal) {
                if (Array.isArray(singleVal)) {
                    list = singleVal;
                } else if (typeof singleVal === 'string') {
                    const trimmed = singleVal.trim();
                    if (trimmed.startsWith('[') && trimmed.endsWith(']')) {
                        try {
                            const parsed = JSON.parse(trimmed);
                            if (Array.isArray(parsed)) list = parsed;
                        } catch (e) {
                            try {
                                const sanitized = trimmed.replace(/'/g, '"');
                                const parsed = JSON.parse(sanitized);
                                if (Array.isArray(parsed)) list = parsed;
                            } catch (e2) {}
                        }
                    }
                    if (list.length === 0 && trimmed.length > 0 && trimmed !== '[]' && trimmed !== '""' && trimmed !== 'null') {
                        list = [trimmed];
                    }
                }
            }
            return list.map(item => {
                if (!item) return '';
                let s = typeof item === 'string' ? item : (item.url || item.path || '');
                s = s.trim().replace(/^["']|["']$/g, '');
                return s;
            }).filter(s => s && s.length > 0 && s !== '[]' && !s.startsWith('['));
        }

        function formatModalMediaUrl(rawPath) {
            if (!rawPath) return '';
            let clean = String(rawPath).trim().replace(/^["']|["']$/g, '');
            if (clean.startsWith('http://') || clean.startsWith('https://') || clean.startsWith('blob:') || clean.startsWith('data:')) {
                return clean;
            }
            if (!clean.startsWith('/')) {
                return '/' + clean;
            }
            return clean;
        }

        const existingPhotos = resolveModalMediaList(bulletin.photo_paths, bulletin.photo_path);
        const existingVideos = resolveModalMediaList(bulletin.video_paths, bulletin.video_path);

        existingPhotos.forEach((p, idx) => {
            if (!p || p === '[]' || p.startsWith('[')) return;
            const normalizedUrl = formatModalMediaUrl(p);
            const filename = normalizedUrl.split('/').pop() || `Photo ${idx + 1}`;
            modalMediaItems.push({
                id: 'existing-photo-' + idx,
                type: 'image',
                url: normalizedUrl,
                path: p,
                name: filename,
                size: 0,
                isExisting: true
            });
        });

        existingVideos.forEach((v, idx) => {
            if (!v || v === '[]' || v.startsWith('[')) return;
            const normalizedUrl = formatModalMediaUrl(v);
            const filename = normalizedUrl.split('/').pop() || `Video ${idx + 1}`;
            modalMediaItems.push({
                id: 'existing-video-' + idx,
                type: 'video',
                url: normalizedUrl,
                path: v,
                name: filename,
                size: 0,
                isExisting: true
            });
        });

        modalCurrentSlide = 0;
        syncModalMediaInputs();
        renderModalCarousel();
        renderModalMediaGalleryGrid();
        updateModalCounters();

        goToPublishStep(2);

        modal.classList.remove('hidden');
        setTimeout(() => {
            modal.classList.remove('opacity-0');
            box.classList.remove('scale-95');
            box.classList.add('scale-100');
        }, 20);
    }

    function closePublishNoticeModal() {
        const modal = document.getElementById('publishNoticeModal');
        const box = document.getElementById('publishNoticeModalBox');
        if (!modal || !box) return;
        
        modal.classList.add('opacity-0');
        box.classList.remove('scale-100');
        box.classList.add('scale-95');
        setTimeout(() => {
            modal.classList.add('hidden');
            currentModalMode = 'create';
            currentEditBulletinId = null;
        }, 300);
    }

    function goToPublishStep(step) {
        const step1 = document.getElementById('publishStep1');
        const step2 = document.getElementById('publishNoticeForm');
        const mainTitle = document.getElementById('modalMainTitle');
        const mainSubtitle = document.getElementById('modalMainSubtitle');

        if (step === 1) {
            if (step1) step1.classList.remove('hidden');
            if (step2) step2.classList.add('hidden');
            if (mainTitle) mainTitle.textContent = currentModalMode === 'edit' ? 'Change Publication Type' : 'Publish Notice';
            if (mainSubtitle) mainSubtitle.textContent = 'Choose how you would like to publish the bulletin.';
        } else {
            if (step1) step1.classList.add('hidden');
            if (step2) step2.classList.remove('hidden');
            if (mainTitle) mainTitle.textContent = currentModalMode === 'edit' ? 'Edit Bulletin Details' : 'Notice Details';
            if (mainSubtitle) mainSubtitle.textContent = currentModalMode === 'edit' ? 'Update notice parameters and media attachments.' : 'Provide notice details below to broadcast.';
        }
    }

    function selectPublishType(type) {
        currentPublishType = type;
        const publishTypeInput = document.getElementById('publishTypeInput');
        if (publishTypeInput) publishTypeInput.value = type;

        const manualPanel = document.getElementById('manualPublishPanel');
        const urlPanel = document.getElementById('urlPublishPanel');
        const selectedBadge = document.getElementById('selectedTypeBadge');
        
        const advisorySelect = document.getElementById('publishCategoryAdvisory');
        const newsBadge = document.getElementById('publishCategoryNewsBadge');
        const fbUrlInput = document.getElementById('publishFacebookUrl');
        const publishBody = document.getElementById('publishBody');
        const publishTitle = document.getElementById('publishTitle');
        const urlTitleInput = document.getElementById('urlTitleInput');

        if (type === 'url') {
            if (manualPanel) manualPanel.classList.add('hidden');
            if (urlPanel) urlPanel.classList.remove('hidden');

            if (selectedBadge) {
                selectedBadge.textContent = 'URL Post';
                selectedBadge.className = 'px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-[#1877F2]/10 text-[#1877F2] border border-[#1877F2]/20';
            }

            if (advisorySelect) {
                advisorySelect.classList.add('hidden');
                advisorySelect.name = "category_inactive";
                advisorySelect.disabled = true;
            }

            if (fbUrlInput) fbUrlInput.required = true;
            if (publishBody) publishBody.required = false;

            if (urlTitleInput && publishTitle) {
                urlTitleInput.name = "title";
                publishTitle.name = "title_inactive";
                urlTitleInput.required = true;
                publishTitle.required = false;
            }

            // Set hidden category for URL post
            setFormCategory('General Announcement');
            if (fbUrlInput && fbUrlInput.value) {
                handleFacebookUrlInput(fbUrlInput.value);
            }
        } else {
            if (manualPanel) manualPanel.classList.remove('hidden');
            if (urlPanel) urlPanel.classList.add('hidden');

            if (fbUrlInput) fbUrlInput.required = false;
            if (publishBody) publishBody.required = true;

            if (urlTitleInput && publishTitle) {
                publishTitle.name = "title";
                urlTitleInput.name = "title_inactive";
                publishTitle.required = true;
                urlTitleInput.required = false;
            }

            if (type === 'advisory') {
                if (advisorySelect) {
                    advisorySelect.classList.remove('hidden');
                    advisorySelect.name = "category";
                    advisorySelect.disabled = false;
                }
                if (newsBadge) newsBadge.classList.add('hidden');

                const newsCatHidden = document.getElementById('hiddenNewsCategory');
                if (newsCatHidden) {
                    newsCatHidden.disabled = true;
                    newsCatHidden.name = "category_inactive";
                }

                if (selectedBadge) {
                    selectedBadge.textContent = 'Public Advisory';
                    selectedBadge.className = 'px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-amber-500/10 text-amber-500 border border-amber-500/20';
                }
            } else if (type === 'news') {
                if (advisorySelect) {
                    advisorySelect.classList.add('hidden');
                    advisorySelect.name = "category_inactive";
                    advisorySelect.disabled = true;
                }
                if (newsBadge) newsBadge.classList.remove('hidden');

                setFormCategory('General Announcement');

                if (selectedBadge) {
                    selectedBadge.textContent = 'News & Release';
                    selectedBadge.className = 'px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-pnp-blue/10 text-pnp-blue dark:text-blue-300 border border-pnp-blue/20';
                }
            }
        }

        updateModalCounters();
        goToPublishStep(2);
    }

    function handleFacebookUrlInput(url) {
        const previewBox = document.getElementById('fbUrlPreviewBox');
        const iframeContainer = document.getElementById('fbIframeContainer');
        const urlTitleInput = document.getElementById('urlTitleInput');

        if (!url || !url.trim() || (!url.includes('facebook.com') && !url.includes('fb.watch') && !url.includes('fb.me'))) {
            if (previewBox) previewBox.classList.add('hidden');
            return;
        }

        const cleanUrl = url.trim();
        const isVideo = /video|watch|reel/i.test(cleanUrl);
        const pluginType = isVideo ? 'video.php' : 'post.php';
        const embedSrc = `https://www.facebook.com/plugins/${pluginType}?href=${encodeURIComponent(cleanUrl)}&show_text=true&width=500`;

        if (iframeContainer) {
            iframeContainer.innerHTML = `<iframe src="${embedSrc}" width="100%" height="100%" style="border:none;overflow:hidden;min-height:300px;" scrolling="no" frameborder="0" allowfullscreen="true" allow="autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share" class="w-full h-full rounded-xl"></iframe>`;
        }

        if (previewBox) previewBox.classList.remove('hidden');

        // Auto-set Category to General Announcement
        setFormCategory('General Announcement');

        // Auto-suggest title if empty
        if (urlTitleInput && !urlTitleInput.value.trim()) {
            urlTitleInput.value = 'Official PNP Facebook Advisory Update';
            updateModalCounters();
        }
    }

    function setFormCategory(catVal) {
        let hiddenCat = document.getElementById('hiddenNewsCategory');
        if (!hiddenCat) {
            hiddenCat = document.createElement('input');
            hiddenCat.type = 'hidden';
            hiddenCat.id = 'hiddenNewsCategory';
            document.getElementById('publishNoticeForm').appendChild(hiddenCat);
        }
        hiddenCat.name = 'category';
        hiddenCat.value = catVal;
        hiddenCat.disabled = false;

        const advisorySelect = document.getElementById('publishCategoryAdvisory');
        if (advisorySelect) {
            advisorySelect.name = 'category_inactive';
            advisorySelect.disabled = true;
        }
    }

    function updateModalCounters() {
        const isUrl = currentPublishType === 'url';
        const titleInput = isUrl ? document.getElementById('urlTitleInput') : document.getElementById('publishTitle');
        const bodyInput = document.getElementById('publishBody');

        const titleCounter = isUrl ? document.getElementById('urlTitleCounter') : document.getElementById('publishTitleCounter');
        const bodyCounter = document.getElementById('publishBodyCounter');

        const titleErrorMsg = isUrl ? document.getElementById('urlTitleError') : document.getElementById('publishTitleError');
        const bodyErrorMsg = document.getElementById('publishBodyError');

        if (titleInput && titleCounter) {
            const letters = titleInput.value ? titleInput.value.length : 0;
            titleCounter.textContent = `${letters} / 100 letters`;

            if (letters === 0) {
                if (titleErrorMsg) {
                    titleErrorMsg.textContent = 'Notice title is required (at least 3 letters).';
                    titleErrorMsg.classList.remove('hidden');
                    titleErrorMsg.className = 'text-[11px] font-bold text-red-500 mt-1';
                }
                titleInput.classList.add('border-red-500', 'ring-2', 'ring-red-500/20');
                titleInput.classList.remove('border-emerald-500', 'ring-emerald-500/20');
                titleCounter.classList.add('text-red-500');
                titleCounter.classList.remove('text-slate-400', 'text-emerald-500');
            } else if (letters < 3) {
                if (titleErrorMsg) {
                    titleErrorMsg.textContent = `Title is too short. Minimum 3 letters required (currently ${letters} letters).`;
                    titleErrorMsg.classList.remove('hidden');
                    titleErrorMsg.className = 'text-[11px] font-bold text-red-500 mt-1';
                }
                titleInput.classList.add('border-red-500', 'ring-2', 'ring-red-500/20');
                titleInput.classList.remove('border-emerald-500', 'ring-emerald-500/20');
                titleCounter.classList.add('text-red-500');
                titleCounter.classList.remove('text-slate-400', 'text-emerald-500');
            } else if (letters > 100) {
                if (titleErrorMsg) {
                    titleErrorMsg.textContent = `Title exceeds maximum limit of 100 letters (currently ${letters} letters).`;
                    titleErrorMsg.classList.remove('hidden');
                    titleErrorMsg.className = 'text-[11px] font-bold text-red-500 mt-1';
                }
                titleInput.classList.add('border-red-500', 'ring-2', 'ring-red-500/20');
                titleInput.classList.remove('border-emerald-500', 'ring-emerald-500/20');
                titleCounter.classList.add('text-red-500');
                titleCounter.classList.remove('text-slate-400', 'text-emerald-500');
            } else {
                if (titleErrorMsg) {
                    titleErrorMsg.textContent = '✓ Title length is valid.';
                    titleErrorMsg.classList.remove('hidden');
                    titleErrorMsg.className = 'text-[11px] font-bold text-emerald-500 mt-1';
                }
                titleInput.classList.remove('border-red-500', 'ring-red-500/20');
                titleInput.classList.add('border-emerald-500', 'ring-2', 'ring-emerald-500/20');
                titleCounter.classList.remove('text-red-500');
                titleCounter.classList.add('text-emerald-500');
            }
        }

        if (!isUrl && bodyInput && bodyCounter) {
            const letters = bodyInput.value ? bodyInput.value.length : 0;
            bodyCounter.textContent = `${letters} / 5,000 letters`;

            if (letters === 0) {
                if (bodyErrorMsg) {
                    bodyErrorMsg.textContent = 'Description is required (at least 10 letters).';
                    bodyErrorMsg.classList.remove('hidden');
                    bodyErrorMsg.className = 'text-[11px] font-bold text-red-500 mt-1';
                }
                bodyInput.classList.add('border-red-500', 'ring-2', 'ring-red-500/20');
                bodyInput.classList.remove('border-emerald-500', 'ring-emerald-500/20');
                bodyCounter.classList.add('text-red-500');
                bodyCounter.classList.remove('text-slate-400', 'text-emerald-500');
            } else if (letters < 10) {
                if (bodyErrorMsg) {
                    bodyErrorMsg.textContent = `Description is too short. Minimum 10 letters required (currently ${letters} letters).`;
                    bodyErrorMsg.classList.remove('hidden');
                    bodyErrorMsg.className = 'text-[11px] font-bold text-red-500 mt-1';
                }
                bodyInput.classList.add('border-red-500', 'ring-2', 'ring-red-500/20');
                bodyInput.classList.remove('border-emerald-500', 'ring-emerald-500/20');
                bodyCounter.classList.add('text-red-500');
                bodyCounter.classList.remove('text-slate-400', 'text-emerald-500');
            } else if (letters > 5000) {
                if (bodyErrorMsg) {
                    bodyErrorMsg.textContent = `Description exceeds limit of 5,000 letters (currently ${letters} letters).`;
                    bodyErrorMsg.classList.remove('hidden');
                    bodyErrorMsg.className = 'text-[11px] font-bold text-red-500 mt-1';
                }
                bodyInput.classList.add('border-red-500', 'ring-2', 'ring-red-500/20');
                bodyInput.classList.remove('border-emerald-500', 'ring-emerald-500/20');
                bodyCounter.classList.add('text-red-500');
                bodyCounter.classList.remove('text-slate-400', 'text-emerald-500');
            } else {
                if (bodyErrorMsg) {
                    bodyErrorMsg.textContent = '✓ Description length is valid.';
                    bodyErrorMsg.classList.remove('hidden');
                    bodyErrorMsg.className = 'text-[11px] font-bold text-emerald-500 mt-1';
                }
                bodyInput.classList.remove('border-red-500', 'ring-red-500/20');
                bodyInput.classList.add('border-emerald-500', 'ring-2', 'ring-emerald-500/20');
                bodyCounter.classList.remove('text-red-500');
                bodyCounter.classList.add('text-emerald-500');
            }
        }
    }

    let modalMediaItems = [];
    let modalCurrentSlide = 0;

    function handleModalMediaSelect(files) {
        if (!files || files.length === 0) return;

        let rejected = [];
        let videoPromises = [];

        Array.from(files).forEach(file => {
            if (modalMediaItems.length + videoPromises.length >= 10) {
                rejected.push(`"${file.name}" (Limit reached: Maximum 10 media files combined)`);
                return;
            }

            if (file.type.startsWith('image/')) {
                if (file.size > 5 * 1024 * 1024) {
                    rejected.push(`Image "${file.name}" (${(file.size/(1024*1024)).toFixed(1)}MB exceeds 5MB limit)`);
                } else {
                    const url = URL.createObjectURL(file);
                    modalMediaItems.push({
                        id: Date.now() + Math.random(),
                        file: file,
                        type: 'image',
                        url: url,
                        name: file.name,
                        size: file.size
                    });
                }
            } else if (file.type.startsWith('video/')) {
                if (file.size > 25 * 1024 * 1024) {
                    rejected.push(`Video "${file.name}" (${(file.size/(1024*1024)).toFixed(1)}MB exceeds 25MB limit)`);
                } else {
                    const p = new Promise(resolve => {
                        const tempVid = document.createElement('video');
                        tempVid.preload = 'metadata';
                        tempVid.src = URL.createObjectURL(file);
                        tempVid.onloadedmetadata = function() {
                            if (tempVid.duration > 180) { // > 3 mins
                                URL.revokeObjectURL(tempVid.src);
                                rejected.push(`Video "${file.name}" (${(tempVid.duration/60).toFixed(1)} mins long - Movie videos exceeding 3 minutes are prohibited)`);
                            } else {
                                modalMediaItems.push({
                                    id: Date.now() + Math.random(),
                                    file: file,
                                    type: 'video',
                                    url: tempVid.src,
                                    name: file.name,
                                    size: file.size,
                                    duration: tempVid.duration
                                });
                            }
                            resolve();
                        };
                        tempVid.onerror = function() {
                            URL.revokeObjectURL(tempVid.src);
                            rejected.push(`Video "${file.name}" (Corrupted or unsupported video format)`);
                            resolve();
                        };
                    });
                    videoPromises.push(p);
                }
            }
        });

        Promise.all(videoPromises).then(() => {
            if (rejected.length > 0) {
                alert(`Upload Notice:\n• ` + rejected.join('\n• '));
            }

            if (modalMediaItems.length > 0) {
                modalCurrentSlide = modalMediaItems.length - 1;
            }

            syncModalMediaInputs();
            renderModalCarousel();

            const input = document.getElementById('modalMediaInput');
            if (input) input.value = '';
        });
    }

    function syncModalMediaInputs() {
        const photosInput = document.getElementById('modalPhotos');
        const videosInput = document.getElementById('modalVideos');
        const existingPhotosInput = document.getElementById('modalExistingPhotos');
        const existingVideosInput = document.getElementById('modalExistingVideos');

        const dtPhotos = new DataTransfer();
        const dtVideos = new DataTransfer();
        const remainingExistingPhotos = [];
        const remainingExistingVideos = [];

        modalMediaItems.forEach(item => {
            if (item.isExisting) {
                if (item.type === 'image') remainingExistingPhotos.push(item.path);
                else if (item.type === 'video') remainingExistingVideos.push(item.path);
            } else if (item.file) {
                if (item.type === 'image') dtPhotos.items.add(item.file);
                else if (item.type === 'video') dtVideos.items.add(item.file);
            }
        });

        if (photosInput) photosInput.files = dtPhotos.files;
        if (videosInput) videosInput.files = dtVideos.files;
        if (existingPhotosInput) existingPhotosInput.value = JSON.stringify(remainingExistingPhotos);
        if (existingVideosInput) existingVideosInput.value = JSON.stringify(remainingExistingVideos);
    }

    function renderModalCarousel() {
        const previewBox = document.getElementById('modalMediaPreviewBox');
        const placeholder = document.getElementById('modalMediaPlaceholder');
        const img = document.getElementById('modalPreviewImg');
        const vid = document.getElementById('modalPreviewVid');
        const badge = document.getElementById('modalCarouselBadge');
        const dotsContainer = document.getElementById('modalCarouselDots');
        const infoEl = document.getElementById('modalCarouselInfo');
        const prevBtn = document.getElementById('modalPrevSlideBtn');
        const nextBtn = document.getElementById('modalNextSlideBtn');
        const clearBtn = document.getElementById('modalClearMediaBtn');
        const addBtnText = document.getElementById('modalAddMediaBtnText');
        const addBtn = document.getElementById('modalAddMediaBtn');

        if (modalMediaItems.length === 0) {
            if (previewBox) previewBox.classList.add('hidden');
            if (placeholder) placeholder.classList.remove('hidden');
            if (clearBtn) clearBtn.classList.add('hidden');
            if (addBtnText) addBtnText.textContent = 'Choose / Add Media Files';
            if (addBtn) addBtn.disabled = false;
            return;
        }

        if (placeholder) placeholder.classList.add('hidden');
        if (previewBox) previewBox.classList.remove('hidden');
        if (clearBtn) clearBtn.classList.remove('hidden');

        if (modalCurrentSlide >= modalMediaItems.length) {
            modalCurrentSlide = Math.max(0, modalMediaItems.length - 1);
        }

        const currentItem = modalMediaItems[modalCurrentSlide];

        if (currentItem.type === 'image') {
            if (vid) { vid.classList.add('hidden'); vid.pause(); }
            if (img) { img.src = currentItem.url; img.classList.remove('hidden'); }
        } else {
            if (img) img.classList.add('hidden');
            if (vid) { vid.src = currentItem.url; vid.classList.remove('hidden'); }
        }

        if (badge) {
            const icon = currentItem.type === 'image' ? '<i class="fas fa-image text-emerald-400 mr-1.5"></i>' : '<i class="fas fa-film text-amber-400 mr-1.5"></i>';
            badge.innerHTML = `${icon}<span>${currentItem.type.toUpperCase()} (${modalCurrentSlide + 1}/${modalMediaItems.length})</span>`;
        }

        if (infoEl) {
            if (currentItem.isExisting) {
                infoEl.textContent = `${currentItem.name} (Attached Media)`;
            } else {
                const sizeMB = (currentItem.size / (1024 * 1024)).toFixed(1);
                const durText = currentItem.duration ? ` • ${Math.round(currentItem.duration)}s clip` : '';
                infoEl.textContent = `${currentItem.name} (${sizeMB} MB${durText})`;
            }
        }

        if (modalMediaItems.length > 1) {
            if (prevBtn) prevBtn.classList.remove('hidden');
            if (nextBtn) nextBtn.classList.remove('hidden');
        } else {
            if (prevBtn) prevBtn.classList.add('hidden');
            if (nextBtn) nextBtn.classList.add('hidden');
        }

        if (dotsContainer) {
            if (modalMediaItems.length > 1) {
                dotsContainer.innerHTML = modalMediaItems.map((_, idx) => `
                    <button type="button" onclick="goToModalSlide(${idx}, event)" class="h-2 rounded-full transition-all cursor-pointer ${idx === modalCurrentSlide ? 'bg-pnp-blue dark:bg-pnp-gold w-6' : 'bg-gray-300 dark:bg-slate-700 hover:bg-gray-400 w-2'}" title="Slide ${idx + 1}"></button>
                `).join('');
                dotsContainer.classList.remove('hidden');
            } else {
                dotsContainer.innerHTML = '';
                dotsContainer.classList.add('hidden');
            }
        }

        if (addBtnText) {
            if (modalMediaItems.length >= 10) {
                addBtnText.textContent = 'Limit Reached (10/10 Files)';
                if (addBtn) addBtn.disabled = true;
            } else {
                addBtnText.textContent = `Add Media (${modalMediaItems.length}/10)`;
                if (addBtn) addBtn.disabled = false;
            }
        }

        const modalViewGalleryBtn = document.getElementById('modalViewGalleryBtn');
        const modalGalleryBtnCount = document.getElementById('modalGalleryBtnCount');
        const modalQuickGalleryLink = document.getElementById('modalQuickGalleryLink');
        if (modalViewGalleryBtn) {
            if (modalMediaItems.length > 0) {
                modalViewGalleryBtn.classList.remove('hidden');
                if (modalGalleryBtnCount) modalGalleryBtnCount.textContent = modalMediaItems.length;
            } else {
                modalViewGalleryBtn.classList.add('hidden');
            }
        }
        if (modalQuickGalleryLink) {
            if (modalMediaItems.length > 0) modalQuickGalleryLink.classList.remove('hidden');
            else modalQuickGalleryLink.classList.add('hidden');
        }

        renderModalThumbnailsStrip();

        // Sync attached file list in urlPublishPanel if present
        const urlFilesContainer = document.getElementById('urlAttachedFilesContainer');
        const urlFilesList = document.getElementById('urlAttachedFilesList');
        const urlAddBtnText = document.getElementById('urlAddFileBtnText');

        if (urlFilesContainer && urlFilesList) {
            if (modalMediaItems.length === 0) {
                urlFilesContainer.classList.add('hidden');
                if (urlAddBtnText) urlAddBtnText.textContent = 'Choose / Upload File';
            } else {
                urlFilesContainer.classList.remove('hidden');
                if (urlAddBtnText) urlAddBtnText.textContent = `Attached (${modalMediaItems.length}/10) - Add More`;
                urlFilesList.innerHTML = modalMediaItems.map((item, idx) => `
                    <div class="flex items-center justify-between p-2.5 bg-white dark:bg-slate-900 rounded-xl border border-gray-150 dark:border-slate-800 text-xs shadow-sm">
                        <div class="flex items-center space-x-2.5 truncate mr-2">
                            <i class="${item.type === 'image' ? 'fas fa-image text-emerald-500' : 'fas fa-film text-amber-500'} text-sm"></i>
                            <span class="font-bold text-slate-700 dark:text-slate-200 truncate">${item.name}</span>
                            ${item.isExisting ? '<span class="text-[10px] font-bold text-pnp-blue dark:text-pnp-gold">(Saved)</span>' : `<span class="text-[10px] font-mono font-bold text-slate-400">(${(item.size/(1024*1024)).toFixed(1)}MB)</span>`}
                        </div>
                        <button type="button" onclick="removeUrlModalMediaItem(${idx})" class="text-red-500 hover:text-white hover:bg-red-500 text-xs px-2.5 py-1 rounded-lg transition flex items-center space-x-1 cursor-pointer" title="Remove file">
                            <i class="fas fa-trash-alt text-[10px]"></i>
                            <span class="text-[10px] font-bold">Remove</span>
                        </button>
                    </div>
                `).join('');
            }
        }
    }

    function removeUrlModalMediaItem(idx) {
        if (idx >= 0 && idx < modalMediaItems.length) {
            const removed = modalMediaItems[idx];
            if (removed && removed.url && !removed.isExisting && String(removed.url).startsWith('blob:')) {
                URL.revokeObjectURL(removed.url);
            }
            modalMediaItems.splice(idx, 1);
            if (modalCurrentSlide >= modalMediaItems.length) {
                modalCurrentSlide = Math.max(0, modalMediaItems.length - 1);
            }
            syncModalMediaInputs();
            renderModalCarousel();
            updateModalCounters();
        }
    }

    function trashCurrentModalMediaItem(e) {
        if (e) e.stopPropagation();
        if (modalMediaItems.length === 0) return;

        const removedItem = modalMediaItems[modalCurrentSlide];
        if (removedItem && removedItem.url && !removedItem.isExisting && String(removedItem.url).startsWith('blob:')) {
            URL.revokeObjectURL(removedItem.url);
        }

        modalMediaItems.splice(modalCurrentSlide, 1);

        if (modalCurrentSlide >= modalMediaItems.length) {
            modalCurrentSlide = Math.max(0, modalMediaItems.length - 1);
        }

        syncModalMediaInputs();
        renderModalCarousel();
        updateModalCounters();
    }

    function navigateModalCarousel(dir, e) {
        if (e) e.stopPropagation();
        if (modalMediaItems.length <= 1) return;
        modalCurrentSlide = (modalCurrentSlide + dir + modalMediaItems.length) % modalMediaItems.length;
        renderModalCarousel();
    }

    function goToModalSlide(idx, e) {
        if (e) e.stopPropagation();
        modalCurrentSlide = idx;
        renderModalCarousel();
    }

    function clearModalMedia() {
        modalMediaItems.forEach(item => {
            if (item.url && !item.isExisting && String(item.url).startsWith('blob:')) {
                URL.revokeObjectURL(item.url);
            }
        });
        modalMediaItems = [];
        modalCurrentSlide = 0;

        const photosInput = document.getElementById('modalPhotos');
        const videosInput = document.getElementById('modalVideos');
        const mediaInput = document.getElementById('modalMediaInput');
        const existingPhotosInput = document.getElementById('modalExistingPhotos');
        const existingVideosInput = document.getElementById('modalExistingVideos');

        if (photosInput) photosInput.value = '';
        if (videosInput) videosInput.value = '';
        if (mediaInput) mediaInput.value = '';
        if (existingPhotosInput) existingPhotosInput.value = '[]';
        if (existingVideosInput) existingVideosInput.value = '[]';

        renderModalCarousel();
        renderModalMediaGalleryGrid();
        closeModalMediaGalleryModal();
    }

    // Render Inline Thumbnails Strip under carousel
    function renderModalThumbnailsStrip() {
        const strip = document.getElementById('modalThumbnailsStrip');
        if (!strip) return;

        if (modalMediaItems.length === 0) {
            strip.innerHTML = '';
            strip.classList.add('hidden');
            return;
        }

        strip.classList.remove('hidden');
        strip.innerHTML = modalMediaItems.map((item, idx) => `
            <div onclick="goToModalSlide(${idx}, event)" class="relative shrink-0 w-12 h-12 rounded-lg overflow-hidden border-2 cursor-pointer transition-all duration-200 group ${idx === modalCurrentSlide ? 'border-pnp-blue ring-2 ring-pnp-blue/30 scale-105' : 'border-gray-200 dark:border-slate-700 opacity-70 hover:opacity-100'}">
                ${item.type === 'image' 
                    ? `<img src="${item.url}" class="w-full h-full object-cover">` 
                    : `<div class="w-full h-full bg-slate-900 flex items-center justify-center text-white"><i class="fas fa-play text-[9px]"></i></div>`}
                <button type="button" onclick="removeModalGalleryItem(${idx}, event)" class="absolute top-0.5 right-0.5 w-4 h-4 bg-red-600 hover:bg-red-700 text-white rounded text-[8px] flex items-center justify-center shadow opacity-0 group-hover:opacity-100 transition" title="Delete file">
                    <i class="fas fa-trash-alt"></i>
                </button>
                <span class="absolute bottom-0.5 left-0.5 px-1 py-0.2 bg-black/70 text-white text-[7px] font-bold uppercase rounded">
                    ${idx + 1}
                </span>
            </div>
        `).join('');
    }

    // Media Gallery Modal for Publish/Edit Notice (View all in one container)
    function openModalMediaGalleryModal() {
        const modal = document.getElementById('publishMediaGalleryModal');
        if (!modal) return;
        renderModalMediaGalleryGrid();
        modal.classList.remove('hidden');
        modal.classList.add('flex');
    }

    function closeModalMediaGalleryModal() {
        const modal = document.getElementById('publishMediaGalleryModal');
        if (modal) {
            modal.classList.add('hidden');
            modal.classList.remove('flex');
        }
    }

    function removeModalGalleryItem(idx, e) {
        if (e) e.stopPropagation();
        if (idx < 0 || idx >= modalMediaItems.length) return;

        const removed = modalMediaItems[idx];
        if (removed && removed.url && !removed.isExisting && String(removed.url).startsWith('blob:')) {
            URL.revokeObjectURL(removed.url);
        }

        modalMediaItems.splice(idx, 1);
        if (modalCurrentSlide >= modalMediaItems.length) {
            modalCurrentSlide = Math.max(0, modalMediaItems.length - 1);
        }

        syncModalMediaInputs();
        renderModalCarousel();
        renderModalMediaGalleryGrid();
        updateModalCounters();
    }

    function renderModalMediaGalleryGrid() {
        const grid = document.getElementById('publishMediaGalleryGrid');
        const countText = document.getElementById('publishMediaGalleryCount');
        if (!grid) return;

        if (countText) countText.textContent = `Showing ${modalMediaItems.length} of 10 attached files`;

        if (modalMediaItems.length === 0) {
            grid.innerHTML = `
                <div class="col-span-full py-16 flex flex-col items-center justify-center text-slate-400 dark:text-slate-500">
                    <div class="w-16 h-16 rounded-2xl bg-gray-100 dark:bg-slate-800 flex items-center justify-center text-3xl mb-3">
                        <i class="fas fa-images"></i>
                    </div>
                    <p class="text-sm font-black uppercase tracking-wider text-slate-700 dark:text-slate-200">No Media Files Attached</p>
                    <p class="text-xs mt-1">Upload images or videos to view and manage them here.</p>
                    <button type="button" onclick="document.getElementById('modalMediaInput').click()" class="mt-4 px-6 py-2.5 bg-pnp-blue hover:bg-blue-900 text-white rounded-xl text-xs font-bold uppercase tracking-wider transition">
                        + Add Media Files
                    </button>
                </div>
            `;
            return;
        }

        grid.innerHTML = modalMediaItems.map((item, idx) => `
            <div class="relative group bg-gray-100 dark:bg-slate-950 rounded-2xl overflow-hidden border border-gray-200 dark:border-white/10 shadow-md aspect-square flex flex-col justify-between transition-all hover:shadow-xl">
                <!-- Thumbnail -->
                <div class="w-full h-full flex items-center justify-center overflow-hidden bg-black/40 cursor-pointer" onclick="openModalFullscreenMediaByIndex(${idx})">
                    ${item.type === 'image' 
                        ? `<img src="${item.url}" class="w-full h-full object-cover group-hover:scale-105 transition duration-300">`
                        : `<video src="${item.url}" class="w-full h-full object-cover"></video>`}
                </div>

                <!-- TOP BAR OVERLAY: File Number & Trash Button -->
                <div class="absolute top-2 left-2 right-2 flex justify-between items-center z-20 pointer-events-none">
                    <span class="pointer-events-auto px-2 py-0.5 bg-slate-950/80 backdrop-blur-md rounded-md text-[9px] font-black uppercase text-white shadow">
                        #${idx + 1}
                    </span>
                    <!-- DELETE BUTTON -->
                    <button type="button" onclick="removeModalGalleryItem(${idx}, event)"
                        class="pointer-events-auto w-8 h-8 rounded-lg bg-red-600 hover:bg-red-700 text-white shadow-lg flex items-center justify-center transition-all hover:scale-110 active:scale-95 cursor-pointer border border-red-500/30"
                        title="Delete this file from notice">
                        <i class="fas fa-trash-alt text-xs"></i>
                    </button>
                </div>

                <!-- BOTTOM BAR OVERLAY: Info & Type Badge -->
                <div class="absolute bottom-2 left-2 right-2 flex justify-between items-center z-20 pointer-events-none">
                    <span class="pointer-events-auto px-2 py-0.5 rounded-md text-[8px] font-black uppercase tracking-wider ${item.type === 'image' ? 'bg-emerald-500/90 text-white' : 'bg-amber-500/90 text-white'} shadow">
                        <i class="${item.type === 'image' ? 'fas fa-image' : 'fas fa-film'} mr-1"></i>${item.type}
                    </span>
                    <span class="pointer-events-auto px-2 py-0.5 bg-black/70 backdrop-blur-md rounded-md text-[8px] font-bold text-slate-300 truncate max-w-[90px] shadow">
                        ${item.isExisting ? 'Saved' : (item.size ? (item.size / (1024*1024)).toFixed(1) + 'MB' : '')}
                    </span>
                </div>
            </div>
        `).join('');
    }

    function openModalFullscreenMediaByIndex(idx) {
        if (idx < 0 || idx >= modalMediaItems.length) return;
        goToModalSlide(idx);
        closeModalMediaGalleryModal();
    }

    let pendingNoticePublishForm = false;

    function handlePublishNoticeSubmit(e) {
        e.preventDefault();
        const errorEl = document.getElementById('publishFormError');
        const errorText = document.getElementById('publishFormErrorText');

        if (currentPublishType === 'url') {
            const urlTitle = document.getElementById('urlTitleInput')?.value.trim() || '';
            const urlBody = document.getElementById('urlBodyInput')?.value.trim() || '';
            const mainTitleInput = document.getElementById('publishTitle');
            const mainBodyInput = document.getElementById('publishBody');
            if (mainTitleInput) mainTitleInput.value = urlTitle;
            if (mainBodyInput) mainBodyInput.value = urlBody;
        }

        const titleInput = currentPublishType === 'url' ? document.getElementById('urlTitleInput') : document.getElementById('publishTitle');
        const fbUrlInput = document.getElementById('publishFacebookUrl');
        const bodyInput = document.getElementById('publishBody');

        const titleVal = titleInput ? titleInput.value.trim() : '';
        const fbUrlVal = fbUrlInput ? fbUrlInput.value.trim() : '';
        const bodyVal = bodyInput ? bodyInput.value.trim() : '';

        const titleWords = titleVal.split(/\s+/).filter(Boolean);
        const bodyWords = bodyVal.split(/\s+/).filter(Boolean);

        if (!titleVal || titleWords.length < 3) {
            if (errorText) errorText.textContent = `Validation Error: Title / Name must be at least 3 words long (currently: ${titleWords.length} words / ${titleVal.length} characters).`;
            if (errorEl) errorEl.classList.remove('hidden');
            return false;
        }

        if (titleVal.length > 100 || titleWords.length > 15) {
            if (errorText) errorText.textContent = `Validation Error: Title is too long (maximum 15 words / 100 characters. Currently: ${titleWords.length} words / ${titleVal.length} characters).`;
            if (errorEl) errorEl.classList.remove('hidden');
            return false;
        }

        if (currentPublishType === 'url') {
            if (!fbUrlVal) {
                if (errorText) errorText.textContent = 'Facebook Post URL is required for URL Post.';
                if (errorEl) errorEl.classList.remove('hidden');
                return false;
            }
        } else {
            if (!bodyVal || bodyWords.length < 5 || bodyVal.length < 15) {
                if (errorText) errorText.textContent = `Validation Error: Bulletin body must be at least 5 words long (currently: ${bodyWords.length} words / ${bodyVal.length} characters).`;
                if (errorEl) errorEl.classList.remove('hidden');
                return false;
            }
            if (bodyWords.length > 500 || bodyVal.length > 5000) {
                if (errorText) errorText.textContent = `Validation Error: Bulletin body is too long (maximum 500 words / 5,000 characters. Currently: ${bodyWords.length} words).`;
                if (errorEl) errorEl.classList.remove('hidden');
                return false;
            }
            if (modalMediaItems.length === 0 && !fbUrlVal) {
                if (errorText) errorText.textContent = 'At least 1 picture, video, or Facebook link must be attached to the notice.';
                if (errorEl) errorEl.classList.remove('hidden');
                return false;
            }
            if (modalMediaItems.length > 10) {
                if (errorText) errorText.textContent = `Media limit exceeded: Maximum 10 media files allowed (currently ${modalMediaItems.length} files attached).`;
                if (errorEl) errorEl.classList.remove('hidden');
                return false;
            }
        }

        if (errorEl) errorEl.classList.add('hidden');

        // Show Confirmation Modal
        openPublishConfirmationModal({
            title: titleVal,
            type: currentPublishType === 'advisory' ? 'Public Advisory' : (currentPublishType === 'news' ? 'News & Release' : 'Facebook URL Post'),
            category: (currentPublishType === 'news' || currentPublishType === 'url') ? 'General Announcement' : (document.getElementById('publishCategoryAdvisory')?.value || 'Advisory'),
            mediaBadge: modalMediaItems.length > 0 ? `${modalMediaItems.length} file(s) attached (${modalMediaItems.filter(i => i.type === 'image').length} photo(s), ${modalMediaItems.filter(i => i.type === 'video').length} video(s))` : (currentPublishType === 'url' ? 'Facebook URL Attached' : 'No media attached')
        });

        return false;
    }

    function openPublishConfirmationModal(data) {
        const isEdit = currentModalMode === 'edit';
        const modalHeading = document.getElementById('confirmModalHeading');
        const modalSubheading = document.getElementById('confirmModalSubheading');
        const finalBtnText = document.getElementById('finalConfirmPublishBtnText');

        if (modalHeading) modalHeading.textContent = isEdit ? 'Confirm Bulletin Update' : 'Confirm Notice Publication';
        if (modalSubheading) modalSubheading.textContent = isEdit ? 'Please review details before saving updates to this bulletin.' : 'Please review details before broadcasting this bulletin.';
        if (finalBtnText) finalBtnText.textContent = isEdit ? 'Yes, Confirm & Update' : 'Yes, Confirm & Publish';

        document.getElementById('confirmModalTitle').textContent = data.title;
        document.getElementById('confirmModalType').textContent = data.type;
        document.getElementById('confirmModalCategory').textContent = data.category;
        document.getElementById('confirmModalMedia').textContent = data.mediaBadge;

        const modal = document.getElementById('publishConfirmModal');
        if (modal) {
            modal.classList.remove('hidden');
            setTimeout(() => {
                modal.classList.remove('opacity-0');
                modal.firstElementChild.classList.remove('scale-95');
                modal.firstElementChild.classList.add('scale-100');
            }, 20);
        }
    }

    function closePublishConfirmationModal() {
        const modal = document.getElementById('publishConfirmModal');
        if (modal) {
            modal.classList.add('opacity-0');
            modal.firstElementChild.classList.remove('scale-100');
            modal.firstElementChild.classList.add('scale-95');
            setTimeout(() => {
                modal.classList.add('hidden');
            }, 200);
        }
    }

    function confirmAndExecuteSubmit() {
        const btn = document.getElementById('finalConfirmPublishBtn');
        const isEdit = currentModalMode === 'edit';
        if (btn) {
            btn.disabled = true;
            btn.innerHTML = `<i class="fas fa-spinner fa-spin"></i> <span>${isEdit ? 'Updating Bulletin...' : 'Publishing Notice...'}</span>`;
        }
        document.getElementById('publishNoticeForm').submit();
    }
</script>

<!-- Publish Notice Glassmorphism Modal -->
<div id="publishNoticeModal" class="fixed inset-0 z-50 flex items-center justify-center p-3 sm:p-6 md:p-8 bg-slate-950/80 backdrop-blur-md transition-all duration-300 hidden opacity-0">
    <div id="publishNoticeModalBox" class="relative bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-3xl shadow-2xl max-w-6xl xl:max-w-7xl w-full p-6 sm:p-10 lg:p-12 overflow-hidden transform transition-all duration-300 scale-95 flex flex-col max-h-[94vh]">
        <!-- Top Header -->
        <div class="flex items-center justify-between pb-6 border-b border-gray-100 dark:border-slate-800 shrink-0">
            <div class="flex items-center space-x-4">
                <div class="w-12 h-12 bg-pnp-blue text-pnp-gold rounded-2xl flex items-center justify-center font-black text-xl shadow-md border border-pnp-gold/30">
                    <i class="fas fa-bullhorn text-base"></i>
                </div>
                <div>
                    <h3 id="modalMainTitle" class="text-xl font-black text-slate-900 dark:text-white uppercase tracking-wider">Publish Notice</h3>
                    <p id="modalMainSubtitle" class="text-xs text-slate-500 dark:text-slate-400 font-medium mt-0.5">Choose how you would like to publish the bulletin.</p>
                </div>
            </div>
            <button type="button" onclick="closePublishNoticeModal()" class="w-10 h-10 rounded-2xl bg-gray-100 dark:bg-slate-800 hover:bg-gray-200 dark:hover:bg-slate-700 text-slate-400 hover:text-slate-700 dark:hover:text-white transition flex items-center justify-center text-sm cursor-pointer">
                <i class="fas fa-times"></i>
            </button>
        </div>

        <!-- Scrollable Modal Body -->
        <div class="py-6 overflow-y-auto flex-grow custom-modal-scrollbar">
            <!-- STEP 1: Choose Publication Method -->
            <div id="publishStep1" class="space-y-4">
                <p class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-2">Select Publication Type</p>
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <!-- Card 1: Public Advisory -->
                    <div onclick="selectPublishType('advisory')" class="group relative p-8 rounded-3xl bg-gray-50 dark:bg-slate-800/60 border-2 border-gray-100 dark:border-slate-700/80 hover:border-amber-500 dark:hover:border-pnp-gold hover:bg-white dark:hover:bg-slate-800 shadow-sm hover:shadow-2xl cursor-pointer transition-all duration-300 flex flex-col justify-between space-y-6">
                        <div class="flex items-center justify-between">
                            <div class="w-14 h-14 rounded-2xl bg-amber-500/10 text-amber-500 flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                                <i class="fas fa-bullhorn"></i>
                            </div>
                            <span class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-amber-500/10 text-amber-500 border border-amber-500/20">Safety Alert</span>
                        </div>
                        <div>
                            <h4 class="text-lg font-black text-slate-800 dark:text-white uppercase tracking-tight group-hover:text-pnp-blue dark:group-hover:text-pnp-gold transition mb-2">Public Advisory</h4>
                            <p class="text-xs text-slate-500 dark:text-slate-400 leading-relaxed font-medium">Create and publish a public safety advisory with text, images, or video announcements.</p>
                        </div>
                        <div class="pt-2 flex items-center text-xs font-bold text-amber-500 uppercase tracking-wider">
                            <span>Proceed to Advisory</span>
                            <i class="fas fa-arrow-right ml-2 text-xs group-hover:translate-x-1 transition-transform"></i>
                        </div>
                    </div>

                    <!-- Card 2: News & Release -->
                    <div onclick="selectPublishType('news')" class="group relative p-8 rounded-3xl bg-gray-50 dark:bg-slate-800/60 border-2 border-gray-100 dark:border-slate-700/80 hover:border-pnp-blue dark:hover:border-pnp-gold hover:bg-white dark:hover:bg-slate-800 shadow-sm hover:shadow-2xl cursor-pointer transition-all duration-300 flex flex-col justify-between space-y-6">
                        <div class="flex items-center justify-between">
                            <div class="w-14 h-14 rounded-2xl bg-pnp-blue/10 text-pnp-blue dark:text-blue-400 flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                                <i class="fas fa-newspaper"></i>
                            </div>
                            <span class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-pnp-blue/10 text-pnp-blue dark:text-blue-300 border border-pnp-blue/20">Press Release</span>
                        </div>
                        <div>
                            <h4 class="text-lg font-black text-slate-800 dark:text-white uppercase tracking-tight group-hover:text-pnp-blue dark:group-hover:text-pnp-gold transition mb-2">News & Release</h4>
                            <p class="text-xs text-slate-500 dark:text-slate-400 leading-relaxed font-medium">Publish official station news, general announcements, and press statements.</p>
                        </div>
                        <div class="pt-2 flex items-center text-xs font-bold text-pnp-blue dark:text-pnp-gold uppercase tracking-wider">
                            <span>Proceed to News</span>
                            <i class="fas fa-arrow-right ml-2 text-xs group-hover:translate-x-1 transition-transform"></i>
                        </div>
                    </div>

                    <!-- Card 3: URL Post -->
                    <div onclick="selectPublishType('url')" class="group relative p-8 rounded-3xl bg-gray-50 dark:bg-slate-800/60 border-2 border-gray-100 dark:border-slate-700/80 hover:border-[#1877F2] dark:hover:border-blue-400 hover:bg-white dark:hover:bg-slate-800 shadow-sm hover:shadow-2xl cursor-pointer transition-all duration-300 flex flex-col justify-between space-y-6">
                        <div class="flex items-center justify-between">
                            <div class="w-14 h-14 rounded-2xl bg-[#1877F2]/10 text-[#1877F2] flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300">
                                <i class="fab fa-facebook-f"></i>
                            </div>
                            <span class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-[#1877F2]/10 text-[#1877F2] border border-[#1877F2]/20">Facebook Import</span>
                        </div>
                        <div>
                            <h4 class="text-lg font-black text-slate-800 dark:text-white uppercase tracking-tight group-hover:text-[#1877F2] transition mb-2">URL Post</h4>
                            <p class="text-xs text-slate-500 dark:text-slate-400 leading-relaxed font-medium">Publish an existing Facebook post by simply providing its official link URL.</p>
                        </div>
                        <div class="pt-2 flex items-center text-xs font-bold text-[#1877F2] uppercase tracking-wider">
                            <span>Proceed to Facebook URL</span>
                            <i class="fas fa-arrow-right ml-2 text-xs group-hover:translate-x-1 transition-transform"></i>
                        </div>
                    </div>
                </div>
            </div>

            <!-- STEP 2: Publication Form -->
            <form id="publishNoticeForm" action="/admin/bulletins/create" method="POST" enctype="multipart/form-data" class="space-y-6 hidden" onsubmit="handlePublishNoticeSubmit(event)">
                <input type="hidden" name="publish_type" id="publishTypeInput" value="advisory">
                <input type="hidden" name="existing_photos" id="modalExistingPhotos" value="[]">
                <input type="hidden" name="existing_videos" id="modalExistingVideos" value="[]">

                <!-- Header navigation inside form -->
                <div class="flex items-center justify-between pb-3 border-b border-gray-100 dark:border-slate-800">
                    <button type="button" onclick="goToPublishStep(1)" class="inline-flex items-center text-xs font-bold text-slate-500 hover:text-pnp-blue dark:text-slate-400 dark:hover:text-pnp-gold transition cursor-pointer">
                        <i class="fas fa-chevron-left mr-2"></i> Change Publication Type
                    </button>
                    <span id="selectedTypeBadge" class="px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-pnp-blue/10 text-pnp-blue dark:text-pnp-gold border border-pnp-blue/20">
                        Public Advisory
                    </span>
                </div>

                <!-- Alert error banner -->
                <div id="publishFormError" class="hidden p-4 rounded-2xl bg-red-500/10 border border-red-500/20 text-red-500 text-xs font-bold flex items-center space-x-3">
                    <i class="fas fa-exclamation-circle text-base shrink-0"></i>
                    <span id="publishFormErrorText"></span>
                </div>

                <!-- DYNAMIC CONTENT AREA 1: For Advisory & News (2-Column bulletin_form layout) -->
                <div id="manualPublishPanel" class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-10 items-stretch">
                    
                    <!-- LEFT COLUMN: UNIFIED MEDIA DRAG & DROP BOX -->
                    <div class="space-y-4 flex flex-col">
                        <label class="block text-[10px] font-bold text-gray-400 dark:text-slate-400 uppercase tracking-widest">
                            Upload Media <span class="text-slate-400 font-normal">(Images & Videos Optional, Max 10)</span>
                        </label>

                        <div class="flex-1 flex flex-col justify-between bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-2xl p-5">
                            <!-- Dropzone Container -->
                            <div id="modalDropZone" onclick="if(modalMediaItems.length === 0) document.getElementById('modalMediaInput').click()"
                                class="w-full min-h-[300px] lg:min-h-[340px] flex-1 rounded-xl bg-gray-100/80 dark:bg-slate-900 flex items-center justify-center cursor-pointer transition-all duration-300 border-2 border-dashed border-gray-200 dark:border-slate-800 hover:border-pnp-blue p-4 relative group overflow-hidden">
                                
                                <!-- Empty State Placeholder -->
                                <div id="modalMediaPlaceholder" class="flex flex-col items-center justify-center text-center p-4">
                                    <div class="w-16 h-16 rounded-2xl bg-pnp-blue/10 text-pnp-blue dark:text-pnp-gold flex items-center justify-center text-3xl mb-4 group-hover:scale-110 transition duration-300">
                                        <i class="fas fa-cloud-upload-alt"></i>
                                    </div>
                                    <span class="text-sm font-bold text-slate-700 dark:text-slate-200 uppercase tracking-wider mb-1">Drag & Drop or Click to Browse</span>
                                    <p class="text-xs text-slate-400 dark:text-slate-500 font-medium max-w-xs leading-relaxed">Upload up to 10 photos or videos combined (JPG, PNG, WEBP, MP4)</p>
                                </div>

                                <!-- Active Carousel Box -->
                                <div id="modalMediaPreviewBox" class="hidden w-full h-full min-h-[280px] flex flex-col justify-between items-center relative" onclick="event.stopPropagation()">
                                    <!-- Media Display Canvas -->
                                    <div class="relative w-full flex-1 flex items-center justify-center bg-black/40 dark:bg-slate-950/80 rounded-xl overflow-hidden min-h-[220px] p-2">
                                        <!-- Top Header Bar Overlay -->
                                        <div class="absolute top-2 left-2 right-2 flex justify-between items-center z-30 pointer-events-none">
                                            <!-- Badge -->
                                            <span id="modalCarouselBadge" class="pointer-events-auto px-3 py-1 rounded-lg bg-slate-900/85 text-white backdrop-blur-md text-[10px] font-black uppercase tracking-wider border border-white/10 shadow-lg flex items-center">
                                                <i class="fas fa-image text-emerald-400 mr-1.5"></i>
                                                <span>PHOTO (1/1)</span>
                                            </span>

                                            <div class="pointer-events-auto flex items-center space-x-2">
                                                <!-- VIEW ALL IN GRID BUTTON -->
                                                <button type="button" onclick="openModalMediaGalleryModal()" class="px-3 py-1.5 bg-slate-900/90 hover:bg-slate-950 text-pnp-gold rounded-lg shadow-lg hover:scale-105 active:scale-95 transition-all flex items-center space-x-1.5 text-[10px] font-black uppercase tracking-wider cursor-pointer border border-pnp-gold/30" title="View all photos and videos in one container">
                                                    <i class="fas fa-th-large text-xs"></i>
                                                    <span>Grid View</span>
                                                </button>

                                                <!-- TRASH / DELETE ITEM BUTTON -->
                                                <button type="button" id="modalTrashCurrentItemBtn" onclick="trashCurrentModalMediaItem(event)" class="px-3 py-1.5 bg-red-600/90 hover:bg-red-700 text-white rounded-lg shadow-lg hover:scale-105 active:scale-95 transition-all flex items-center space-x-1.5 text-[10px] font-black uppercase tracking-wider cursor-pointer border border-red-500/30" title="Delete this image/video from notice">
                                                    <i class="fas fa-trash-alt text-xs"></i>
                                                    <span>Trash</span>
                                                </button>
                                            </div>
                                        </div>

                                        <!-- Image Slide -->
                                        <img id="modalPreviewImg" class="max-h-[230px] max-w-full object-contain rounded-lg shadow-md hidden">

                                        <!-- Video Slide -->
                                        <video id="modalPreviewVid" controls class="max-h-[230px] max-w-full object-contain rounded-lg shadow-md hidden"></video>

                                        <!-- Prev Slide Chevron -->
                                        <button type="button" id="modalPrevSlideBtn" onclick="navigateModalCarousel(-1, event)" class="absolute left-2 top-1/2 -translate-y-1/2 w-9 h-9 rounded-full bg-slate-900/80 hover:bg-slate-950 text-white flex items-center justify-center transition-all shadow-xl z-30 border border-white/15 cursor-pointer hover:scale-110 hidden" title="Previous Media">
                                            <i class="fas fa-chevron-left text-xs"></i>
                                        </button>

                                        <!-- Next Slide Chevron -->
                                        <button type="button" id="modalNextSlideBtn" onclick="navigateModalCarousel(1, event)" class="absolute right-2 top-1/2 -translate-y-1/2 w-9 h-9 rounded-full bg-slate-900/80 hover:bg-slate-950 text-white flex items-center justify-center transition-all shadow-xl z-30 border border-white/15 cursor-pointer hover:scale-110 hidden" title="Next Media">
                                            <i class="fas fa-chevron-right text-xs"></i>
                                        </button>
                                    </div>

                                    <!-- Dots & File Info Footer -->
                                    <div class="w-full mt-3 flex flex-col items-center space-y-1.5">
                                        <div id="modalCarouselDots" class="flex items-center space-x-1.5 min-h-[12px]"></div>
                                        <p id="modalCarouselInfo" class="text-[11px] font-bold text-slate-500 dark:text-slate-400 truncate max-w-full px-2 text-center"></p>

                                        <!-- Inline Thumbnails Strip: View All Uploaded Pictures & Videos directly on modal -->
                                        <div id="modalThumbnailsStrip" class="w-full pt-2 border-t border-gray-200/60 dark:border-slate-800/80 flex items-center space-x-2 overflow-x-auto pb-1 custom-scrollbar hidden">
                                            <!-- Injected thumbnail previews -->
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Inputs & Control Buttons -->
                            <div class="mt-4 space-y-2">
                                <input type="file" id="modalMediaInput" accept="image/*,video/*" multiple class="hidden" onchange="handleModalMediaSelect(this.files)">
                                <input type="file" name="photos" id="modalPhotos" accept="image/*" multiple class="hidden">
                                <input type="file" name="videos" id="modalVideos" accept="video/*" multiple class="hidden">

                                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                                    <button type="button" id="modalAddMediaBtn" onclick="document.getElementById('modalMediaInput').click()" class="w-full py-3.5 px-4 bg-pnp-blue hover:bg-blue-900 disabled:bg-slate-300 dark:disabled:bg-slate-800 disabled:text-slate-500 text-white rounded-xl text-xs font-bold uppercase tracking-wider transition shadow-sm cursor-pointer disabled:cursor-not-allowed flex items-center justify-center space-x-2">
                                        <i class="fas fa-plus-circle"></i>
                                        <span id="modalAddMediaBtnText">Add Media (0/10)</span>
                                    </button>

                                    <button type="button" id="modalViewGalleryBtn" onclick="openModalMediaGalleryModal()"
                                        class="w-full py-3.5 px-4 bg-slate-800 hover:bg-slate-700 text-pnp-gold rounded-xl text-xs font-bold uppercase tracking-wider transition border border-pnp-gold/20 flex items-center justify-center space-x-2 cursor-pointer hidden">
                                        <i class="fas fa-th-large"></i>
                                        <span>View All (<span id="modalGalleryBtnCount">0</span>)</span>
                                    </button>
                                </div>

                                <button type="button" id="modalClearMediaBtn" onclick="clearModalMedia()" class="w-full py-2.5 px-4 bg-red-500/10 hover:bg-red-500 text-red-500 hover:text-white rounded-xl text-xs font-bold uppercase tracking-wider transition hidden cursor-pointer flex items-center justify-center space-x-2">
                                    <i class="fas fa-trash"></i>
                                    <span>Clear All Media</span>
                                </button>

                                <p class="text-[10px] text-slate-400 dark:text-slate-500 uppercase text-center pt-1">
                                    JPG, PNG, WEBP, MP4 allowed • Up to 10 media files combined
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- RIGHT COLUMN: FORM FIELDS -->
                    <div class="space-y-5 flex flex-col">
                        <!-- TITLE FIELD -->
                        <div class="space-y-1.5">
                            <div class="flex justify-between items-center">
                                <label class="block text-xs font-bold text-slate-700 dark:text-slate-300 uppercase tracking-wider">
                                    Notice Title <span class="text-red-500">*</span>
                                </label>
                                <span id="publishTitleCounter" class="text-[10px] font-mono font-bold text-slate-400 uppercase">
                                    0 / 100 letters
                                </span>
                            </div>
                            <input type="text" name="title" id="publishTitle" required maxlength="100" placeholder="e.g. Heavy Rainfall Warning Advisory" oninput="updateModalCounters()" class="w-full px-4 py-3.5 bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-xl focus:ring-2 focus:ring-pnp-blue dark:focus:ring-pnp-gold outline-none text-xs font-bold text-slate-800 dark:text-white transition">
                            <p id="publishTitleError" class="text-[11px] font-bold text-red-500 mt-1 hidden"></p>
                        </div>

                        <!-- CATEGORY FIELD -->
                        <div class="space-y-1.5">
                            <label class="block text-xs font-bold text-slate-700 dark:text-slate-300 uppercase tracking-wider">
                                Notice Category <span class="text-red-500">*</span>
                            </label>
                            
                            <!-- Category Select for Advisory -->
                            <select name="category" id="publishCategoryAdvisory" class="w-full px-4 py-3.5 bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-xl focus:ring-2 focus:ring-pnp-blue dark:focus:ring-pnp-gold outline-none text-xs font-bold text-slate-800 dark:text-white transition cursor-pointer">
                                <option value="Crime Advisory">Crime Advisory</option>
                                <option value="Traffic Advisory">Traffic Advisory</option>
                                <option value="Cybercrime Advisory">Cybercrime Advisory</option>
                                <option value="Community Awareness">Community Awareness</option>
                            </select>

                            <!-- Locked Category for News -->
                            <div id="publishCategoryNewsBadge" class="hidden w-full px-4 py-3.5 bg-pnp-blue/10 border border-pnp-blue/20 rounded-xl text-xs font-bold text-pnp-blue dark:text-pnp-gold flex items-center justify-between">
                                <div class="flex items-center space-x-2">
                                    <i class="fas fa-newspaper text-sm"></i>
                                    <span>General Announcement (News / Press Release)</span>
                                </div>
                                <span class="text-[10px] uppercase tracking-wider font-black px-2 py-0.5 rounded bg-pnp-blue/20">Locked</span>
                            </div>
                        </div>

                        <!-- BODY TEXTAREA -->
                        <div class="flex-1 flex flex-col space-y-1.5">
                            <div class="flex justify-between items-center">
                                <label class="block text-xs font-bold text-slate-700 dark:text-slate-300 uppercase tracking-wider">
                                    Description / Content <span class="text-red-500">*</span>
                                </label>
                                <span id="publishBodyCounter" class="text-[10px] font-mono font-bold text-slate-400 uppercase">
                                    0 / 5,000 letters
                                </span>
                            </div>
                            <textarea name="body" id="publishBody" rows="8" placeholder="Enter official announcement or advisory contents..." oninput="updateModalCounters()" class="flex-1 w-full px-4 py-3.5 bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-xl focus:ring-2 focus:ring-pnp-blue dark:focus:ring-pnp-gold outline-none text-xs font-medium text-slate-800 dark:text-white transition leading-relaxed"></textarea>
                            <p id="publishBodyError" class="text-[11px] font-bold text-red-500 mt-1 hidden"></p>
                        </div>
                    </div>
                </div>

                <!-- DYNAMIC CONTENT AREA 2: For Separate URL Post Flow -->
                <div id="urlPublishPanel" class="hidden space-y-6">
                    <div class="p-5 rounded-2xl bg-[#1877F2]/10 border border-[#1877F2]/20 flex items-center space-x-4">
                        <div class="w-12 h-12 rounded-2xl bg-[#1877F2] text-white flex items-center justify-center text-2xl shrink-0 shadow-md">
                            <i class="fab fa-facebook-f"></i>
                        </div>
                        <div>
                            <h4 class="text-sm font-black text-[#1877F2] uppercase tracking-wider">Facebook Official Post URL Import</h4>
                            <p class="text-xs text-slate-600 dark:text-slate-300 mt-0.5 font-medium">Link directly to an official post published on the PNP Sta. Cruz Facebook page.</p>
                        </div>
                    </div>

                    <!-- TITLE FIELD FOR URL POST -->
                    <div class="space-y-1.5">
                        <div class="flex justify-between items-center">
                            <label class="block text-xs font-bold text-slate-700 dark:text-slate-300 uppercase tracking-wider">
                                Post Title / Headline <span class="text-red-500">*</span>
                            </label>
                            <span id="urlTitleCounter" class="text-[10px] font-mono font-bold text-slate-400 uppercase">
                                0 / 100 letters
                            </span>
                        </div>
                        <input type="text" id="urlTitleInput" maxlength="100" placeholder="e.g. Official Statement on Community Outreach Operation" oninput="updateModalCounters()" class="w-full px-4 py-3.5 bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-xl focus:ring-2 focus:ring-[#1877F2] outline-none text-xs font-bold text-slate-800 dark:text-white transition">
                        <p id="urlTitleError" class="text-[11px] font-bold text-red-500 mt-1 hidden"></p>
                    </div>

                    <!-- CAPTION / DESCRIPTION FIELD FOR URL POST -->
                    <div class="space-y-1.5">
                        <div class="flex justify-between items-center">
                            <label class="block text-xs font-bold text-slate-700 dark:text-slate-300 uppercase tracking-wider">
                                Post Description / First Paragraph <span class="text-slate-400 font-normal">(Optional)</span>
                            </label>
                            <span id="urlBodyCounter" class="text-[10px] font-mono font-bold text-slate-400 uppercase">
                                0 / 5,000 letters
                            </span>
                        </div>
                        <textarea id="urlBodyInput" rows="3" placeholder="Paste or enter the Facebook caption or first paragraph..." oninput="updateModalCounters()" class="w-full px-4 py-3 bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-xl focus:ring-2 focus:ring-[#1877F2] outline-none text-xs font-medium text-slate-800 dark:text-white transition leading-relaxed"></textarea>
                    </div>

                    <!-- FACEBOOK POST URL FIELD -->
                    <div class="space-y-3">
                        <label class="block text-xs font-bold text-slate-700 dark:text-slate-300 uppercase tracking-wider">
                            Facebook Post URL <span class="text-red-500">*</span>
                        </label>
                        <div class="relative">
                            <i class="fab fa-facebook text-[#1877F2] absolute left-4 top-1/2 -translate-y-1/2 text-base"></i>
                            <input type="url" name="facebook_url" id="publishFacebookUrl" oninput="handleFacebookUrlInput(this.value)" placeholder="https://www.facebook.com/..." class="w-full pl-11 pr-4 py-3.5 bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-xl focus:ring-2 focus:ring-[#1877F2] outline-none text-xs font-semibold text-slate-800 dark:text-white transition">
                        </div>
                        <p class="text-[11px] text-slate-400 font-medium">Paste the exact link to a public post or video from the official PNP Facebook page.</p>

                        <!-- Live Facebook Embedded Media Preview Container -->
                        <div id="fbUrlPreviewBox" class="hidden mt-3 p-3.5 rounded-2xl bg-slate-950 border border-[#1877F2]/30 space-y-2.5 overflow-hidden">
                            <div class="flex items-center justify-between text-[10px] font-black uppercase tracking-wider text-[#1877F2]">
                                <span class="flex items-center space-x-1.5">
                                    <i class="fab fa-facebook text-sm"></i>
                                    <span>Imported Facebook Media Preview</span>
                                </span>
                                <span class="px-2.5 py-0.5 rounded-full bg-[#1877F2]/20 text-[#1877F2]">General Announcement</span>
                            </div>
                            <div id="fbIframeContainer" class="w-full h-[320px] rounded-xl overflow-hidden bg-slate-900 flex items-center justify-center">
                                <!-- Dynamic iframe injected here -->
                            </div>
                        </div>
                    </div>

                    <!-- Facebook Post Info Note -->
                    <div class="p-3.5 rounded-xl bg-blue-50/60 dark:bg-slate-900/60 border border-blue-150 dark:border-slate-800 text-xs font-semibold text-slate-600 dark:text-slate-300 flex items-center space-x-2">
                        <i class="fab fa-facebook text-[#1877F2] text-sm"></i>
                        <span>Facebook post URLs render the official embedded post directly without image file attachments.</span>
                    </div>

                    <!-- Category readout for URL post -->
                    <div class="p-4 rounded-xl bg-gray-50 dark:bg-slate-950 border border-gray-100 dark:border-slate-800 flex items-center justify-between text-xs text-slate-600 dark:text-slate-300 font-bold">
                        <span class="uppercase tracking-wider text-[10px] text-slate-400">Default Category</span>
                        <span class="px-3 py-1 rounded-full bg-pnp-blue/10 text-pnp-blue dark:text-pnp-gold text-[10px] uppercase font-black tracking-wider">General Announcement</span>
                    </div>
                </div>

                <!-- Footer Action Buttons -->
                <div class="pt-5 border-t border-gray-100 dark:border-slate-800 flex items-center justify-between">
                    <div id="modalValidationWarning" class="hidden text-xs font-bold text-red-500 flex items-center space-x-2">
                        <i class="fas fa-exclamation-triangle"></i>
                        <span id="modalValidationText">Title exceeds 100 letters limit.</span>
                    </div>
                    <div class="flex items-center space-x-3 ml-auto">
                        <button type="button" onclick="closePublishNoticeModal()" class="px-6 py-3.5 rounded-xl bg-gray-100 dark:bg-slate-800 hover:bg-gray-200 dark:hover:bg-slate-700 text-slate-600 dark:text-slate-300 text-xs font-bold uppercase tracking-wider transition cursor-pointer">
                            Cancel
                        </button>
                        <button type="submit" id="publishSubmitBtn" class="px-10 py-3.5 rounded-xl bg-pnp-blue hover:bg-pnp-gold text-white hover:text-pnp-blue text-xs font-black uppercase tracking-wider shadow-lg transition-all flex items-center space-x-2 cursor-pointer">
                            <i class="fas fa-paper-plane text-xs"></i>
                            <span id="publishSubmitBtnText">Publish Notice</span>
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Publish Confirmation Dialog Modal -->
<div id="publishConfirmModal" class="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-slate-950/80 backdrop-blur-md transition-all duration-300 hidden opacity-0">
    <div class="relative bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-3xl shadow-2xl max-w-lg w-full p-6 sm:p-8 overflow-hidden transform transition-all duration-300 scale-95 space-y-6">
        <div class="flex items-center space-x-3 text-pnp-blue dark:text-pnp-gold">
            <div class="w-12 h-12 rounded-2xl bg-pnp-blue/10 flex items-center justify-center text-2xl border border-pnp-blue/20">
                <i class="fas fa-question-circle"></i>
            </div>
            <div>
                <h3 id="confirmModalHeading" class="text-lg font-black uppercase tracking-wider text-slate-900 dark:text-white">Confirm Notice Publication</h3>
                <p id="confirmModalSubheading" class="text-xs text-slate-500 dark:text-slate-400 font-medium">Please review details before broadcasting this bulletin.</p>
            </div>
        </div>

        <div class="bg-gray-50 dark:bg-slate-950 border border-gray-200 dark:border-slate-800 rounded-2xl p-4 space-y-3 text-xs">
            <div>
                <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest block">Notice Title</span>
                <span id="confirmModalTitle" class="font-bold text-slate-800 dark:text-white text-sm"></span>
            </div>
            <div class="grid grid-cols-2 gap-3 pt-2 border-t border-gray-200 dark:border-slate-800">
                <div>
                    <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest block">Type</span>
                    <span id="confirmModalType" class="font-bold text-slate-700 dark:text-slate-200"></span>
                </div>
                <div>
                    <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest block">Category</span>
                    <span id="confirmModalCategory" class="font-bold text-pnp-blue dark:text-pnp-gold"></span>
                </div>
            </div>
            <div class="pt-2 border-t border-gray-200 dark:border-slate-800">
                <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest block">Media Attachment</span>
                <span id="confirmModalMedia" class="font-bold text-slate-600 dark:text-slate-300"></span>
            </div>
        </div>

        <div class="flex items-center justify-end space-x-3 pt-2">
            <button type="button" onclick="closePublishConfirmationModal()" class="px-5 py-3 rounded-xl bg-gray-100 dark:bg-slate-800 hover:bg-gray-200 text-slate-600 dark:text-slate-300 text-xs font-bold uppercase tracking-wider transition cursor-pointer">
                Go Back & Edit
            </button>
            <button type="button" onclick="confirmAndExecuteSubmit()" id="finalConfirmPublishBtn" class="px-6 py-3 rounded-xl bg-pnp-blue hover:bg-pnp-gold text-white hover:text-pnp-blue text-xs font-black uppercase tracking-wider shadow-lg transition-all flex items-center space-x-2 cursor-pointer">
                <i class="fas fa-paper-plane"></i>
                <span id="finalConfirmPublishBtnText">Yes, Confirm & Publish</span>
            </button>
        </div>
    </div>
</div>

<!-- ALL UPLOADED MEDIA GALLERY GRID MODAL (FOR PUBLISH / EDIT MODAL) -->
<div id="publishMediaGalleryModal" class="fixed inset-0 bg-slate-950/90 backdrop-blur-md hidden items-center justify-center z-[110] p-4 md:p-8 transition-all duration-300" onclick="closeModalMediaGalleryModal()">
    <div class="relative max-w-6xl w-full h-[90vh] bg-white dark:bg-slate-900 rounded-3xl border border-gray-200 dark:border-white/10 shadow-2xl flex flex-col overflow-hidden" onclick="event.stopPropagation()">
        <!-- GALLERY MODAL HEADER -->
        <div class="p-6 border-b border-gray-150 dark:border-white/10 flex flex-col sm:flex-row sm:items-center justify-between bg-gray-50/80 dark:bg-slate-950 gap-4 shrink-0">
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 rounded-2xl bg-pnp-blue text-pnp-gold flex items-center justify-center border border-pnp-gold/20 shadow-md">
                    <i class="fas fa-images text-sm"></i>
                </div>
                <div>
                    <h3 class="text-base font-black text-slate-900 dark:text-white uppercase tracking-tight leading-none">Attached Media Gallery</h3>
                    <p id="publishMediaGalleryCount" class="text-[10px] text-slate-500 dark:text-slate-400 font-bold uppercase tracking-widest mt-1">Showing 0 of 10 attached files</p>
                </div>
            </div>

            <div class="flex items-center space-x-3">
                <button type="button" onclick="document.getElementById('modalMediaInput').click()" class="px-4 py-2.5 bg-pnp-blue hover:bg-blue-900 text-white rounded-xl text-xs font-bold uppercase tracking-wider transition flex items-center space-x-2 cursor-pointer shadow-md">
                    <i class="fas fa-plus-circle text-xs"></i>
                    <span>+ Add More Files</span>
                </button>
                <button type="button" onclick="closeModalMediaGalleryModal()" class="w-10 h-10 bg-gray-100 hover:bg-gray-200 dark:bg-white/10 dark:hover:bg-white/20 text-slate-600 dark:text-white rounded-xl flex items-center justify-center transition cursor-pointer">
                    <i class="fas fa-times text-sm"></i>
                </button>
            </div>
        </div>

        <!-- GALLERY GRID CONTAINER -->
        <div id="publishMediaGalleryGrid" class="flex-1 p-6 overflow-y-auto custom-scrollbar grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4 sm:gap-6 items-start">
            <!-- Injected cards -->
        </div>

        <!-- GALLERY MODAL FOOTER -->
        <div class="p-5 border-t border-gray-150 dark:border-white/10 bg-gray-50/80 dark:bg-slate-950 flex flex-col sm:flex-row items-center justify-between gap-4 shrink-0">
            <span class="text-[11px] text-slate-500 dark:text-slate-400 font-medium">
                <i class="fas fa-info-circle text-pnp-blue dark:text-pnp-gold mr-1"></i>
                Click the trash icon on any file to delete it. Maximum limit is 10 images/videos.
            </span>
            <div class="flex items-center space-x-3">
                <button type="button" onclick="closeModalMediaGalleryModal()" class="px-8 py-2.5 bg-pnp-blue hover:bg-blue-900 text-white rounded-xl text-xs font-black uppercase tracking-wider transition cursor-pointer shadow-md">
                    Done
                </button>
            </div>
        </div>
    </div>
</div>
